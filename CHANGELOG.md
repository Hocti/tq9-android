# Changelog

TQ9 九万輸入法 (Android) 嘅改動記錄。版本號規則見 `AGENTS.md`
「版本號：每次改完自動加一」一節。

---

## [1.1.0] — 2026-08-25

### 中文九宮格

- **選字翻頁鍵有三種排法**（設定 →「一般 → 選字翻頁」），預設換咗做新嗰個：
  - **大格「下頁」**（新，預設）：兩格闊嗰粒 `0` 成粒都係「下頁」，易撳中好多；
    **長撳同一粒 = 上頁**。呢個狀態下長撳 `0` 嘅成對標點（`「」`）會暫時失效，
    個位改為喺**左上角**細字寫住「上頁」，頁數（`1/10`）就搬去**右上角**。
    離開選字狀態，`「」` 即刻回復正常。
  - **拆兩粒：上頁、下頁** / **下頁、上頁**：照舊拆做兩粒正常闊，左右次序自己揀
    （「下頁、上頁」就係 1.0.15 嗰個排法）。

### AI

- **AI 頁分咗三大類，每類都收埋得**：**語音輸入 (STT)**、**AI 改寫**、**AI 設定**。
  API key 同模型名稱兩邊共用，一齊搬咗入「AI 設定」（連自訂 API 同 profile）。
- **AI 改寫加咗總開關**：熄咗就算入咗 API key，工具列都唔會出粒 ✨。
- **新增 AI 語音輸入**：開咗之後，粒 🎤 由系統內置嘅語音輸入改為交俾 Gemini 辨識。
  - **撳一下開始錄音，再撳一次（或者輕觸畫面）停止**；**撳住 🎤 唔放手**就一路錄，
    放手即停。
  - 錄音同等緊結果嗰陣，**成個鍵盤會變灰兼撳唔到**：錄音時中間出計時器，
    等結果時出載入動畫。
  - **開始錄音、錄音完結、成功、失敗四種唔同嘅提示音。**
  - 送上去嘅唔淨止係段錄音，仲有輸入框而家嘅內容做上下文（純粹畀 AI 知個語境，
    唔會重複輸出）。prompt 可以自己改，預設嗰個已經寫死「只輸出結果、逐字轉錄、
    只用繁體字」。錄音最長 90 秒。
  - **淨係 Gemini 做得到**（要送成段錄音上去），開咗「使用自訂 API」就用唔到，
    個開關會鎖住。

### 設定

- **按鍵震動改咗做 0～3 級 slider**（本來淨係一個開關）：0 = 完全關閉，
  1 = 舊版唯一嗰個力度（最輕），2／3 逐級大力啲，放手嗰陣會震一下畀你試。
  舊設定會自動轉過嚟（本來開住 = 1、閂咗 = 0）。
  部分機款唔支援自訂震幅，嗰啲機淨係感覺到震動時間長短嘅分別。
- **「長按時間」改為逐 10ms 一格**（本來逐 1ms，根本揀唔準）。
- **「一般」分頁最底寫住版本號**（`版本 1.0.16（build 17）`），回報問題嗰陣用得着。

## [1.0.15] — 2026-08-25

### 中文九宮格

- **「下頁」左上角寫住頁數**：選字夠兩頁嗰陣，粒「下頁」左上角出 `1/10`
  （而家第幾頁／總共幾頁，**由 1 起計**），唔使自己數撳咗幾多下。
  冇分頁就照舊喺嗰個位寫長撳提示 `「」`。
- **同音鍵左上角寫住搵緊邊隻字嘅同音**：攤開同音字表嗰陣，成頁都係同音字，
  以前唔知係邊隻字嘅同音。揀完照舊變返「嗰個字正路點打」嘅字碼
  （`Q9Engine.homoWord` 新增，`homoCodeHint` 冇變）。
- **中文本體最少 320dp 闊**（螢幕本身窄過 320dp 就用盡螢幕）：
  左右拖同 `widthScale` 都收唔過呢條線，唔會再拉到粒粒鍵細過隻手指。
- **長撳工具列「靠左／靠右」嗰粒（撳實唔拉）= 一下子拉到最闊**，
  唔使一路拖返出去。（手機直度嘅最闊就係螢幕闊度。）

### 純數字鍵盤

- **左邊加一欄 `+ - * /`**：`-` 由底行搬咗過嚟，計數／打算式唔使再走去符號頁。
- **`0` 右邊加咗粒 `000`**（一次過打三個 0），補返 `-` 讓出嚟嗰個位。
- **闊度同靠左／靠右跟返中文九宮格**（同一個 `PadMetrics`，一樣係 5 欄）——
  以前呢頁自己置中兼且封頂 360dp，中英切換啲鍵會左右彈。

### 表情符號

- **搵 emoji 嗰陣底行淨係剩返「退出表情搜尋」同 ␣**：`?123`、`中`、`⏎`、標點
  喺嗰頁一粒都用唔著。粒退出掣寫明幾隻字，唔再係得個 😀 令人估。
- **搜尋改用單色符號 `⌕`**（`SEARCH_GLYPH`），唔再用彩色 emoji 🔍 ——
  搜尋欄嘅 `⏎` 同 emoji 表嗰粒搵字掣兩處都係。字型冇 `⌕` 就寫返「搜尋」兩隻字。

### 設定頁

- 新增**「使用習慣統計」**一段：`usage_stats.db` 匯出／匯入／清除（重設做新表），
  同埋「**常用字排前**」開關（預設開）。熄咗照樣繼續記數，淨係唔攞嚟排候選字
  （`Prefs.KEY_USAGE_REORDER` → `Q9Engine.usageReorder`）。

## [1.0.13] — 2026-08-25

### 中文九宮格

- **「上頁」變咗粒真掣**：選字夠兩頁嗰陣，兩格闊嘅 `0`（「下頁」）拆做兩粒
  正常闊 —— 左邊照舊係「下頁」，右邊係新嘅「上頁」。
  以前撳住「下頁」向左掃先返到上一頁，難撳到唔知有呢樣嘢，成套 flick
  （`KeyboardBaseView.canFlick` / `onFlick`）刪咗。
- **一個碼都未打嗰陣長撳 `1`~`9`** = 直接開嗰格嘅速選字表，
  唔使再「撳個碼再撳速選掣」。打緊碼就照舊「長撳 = 連撳」（`77x` 撳得返）。
- **選字模式長撳其中一格** = 開嗰個字嘅同音字表，唔使先撳「同音」掣。
  順手執返一個手尾：以前喺選字模式長撳一格會即刻揀咗個字，
  放手嗰下再攞返個數字起新碼（一撳出兩樣嘢）。

### 符號／數字頁

- 左下角兩粒轉鍵盤掣**英行先，跟住先至中**；英文嗰粒由 `ABC` 改寫 `Eng`
  （中文九宮格一路都係寫 `Eng`，而家全部頁一致）。
- 符號第一頁嘅分頁掣由 `=\<` 改寫 **`€£¥`** —— 第二頁頭一行就係啲銀紙符號。

### 上架

- Release 簽名分做兩條 key：side-load（dl／GitHub）照舊用 `~/.android/debug.keystore`，
  Google Play 就行 `./gradlew bundleRelease -Ptq9.upload`，用
  `~/.android/tq9-release.keystore`（keystore 同密碼都唔喺 repo，`.gitignore`
  封晒 `*.keystore` / `*.jks` / `*keystore.properties`）。

## [1.0.9] — 2026-08-24

修 1.0.8 嗰輪嘅手尾。

- **打橫唔會再俾候選字撐高成個鍵盤**：側邊欄嘅高度由 `MATCH_PARENT` 改成寫死
  `PadMetrics.totalHeight`（＝中文九宮格幾高）。`padHolder` 係 `wrap_content`
  嘅 `FrameLayout`，`MATCH_PARENT` 嘅仔加埋個食 weight 嘅候選字 `ScrollView`
  就會愈多字愈高。
- **長撳變體多過螢幕裝得落就一齊迫窄**，一行剛好鋪滿成個闊度
  （`/` 有八個，以前尾幾個推咗出螢幕外面永遠揀唔到）。
- **`,` 長撳加咗 Tab**（`\t`）而且係佢嘅預設項；`.` 長撳預設改成 `;`。
  Tab 冇字形，`variantDisplay()` 畫成 `⇥`，commit 出去仲係真正嘅 `\t`。
- 設定頁刪咗「上方工具列預設」—— 喺鍵盤度撳 ☰／⇄ 已經夠。
- 設定頁「按鍵功能」兩個下拉式選單加咗外框，同時修好一個 bug：
  揀咗之後個掣郁咗但 pref 冇改、上面個 label 仲係舊嗰個，跟住喺另一個選單
  揀返同一樣嘢就會冤枉你「功能重覆」。成因係用咗「跳過第一下 callback」
  嗰招擋 programmatic selection，而第一下幾時 fire 係睇 layout 時序嘅。
  而家改成同 pref 現值比較，另外短撳撞咗長撳嗰陣會將 `NONE` 寫實落 pref
  （以前淨係計出嚟，冇存落去）。

## [1.0.8] — 2026-08-24

### 英文鍵盤大執位

- `a` / `l` 唔再拉長，`asdfghjkl` 九粒一樣闊，兩頭各讓半格空位
  （新 `Key.spacer`，唔入 `boxes`，撳落去會 snap 去隔籬真嗰粒鍵）。
- 刪咗獨立嗰粒 `?`；`,` 由 `zxcvbnm` 行搬落底行頂返個位，`⇧` 同 `⌫` 拉長。
- `/` 搬咗去 space 右邊 —— space 右邊而家順住排 `, . /` 三粒。
- 三粒標點都有長撳 popup（左上角有細字提示）：
  `,` → `< > [ ] { }`、`.` → `' " ; : \` ~`、`/` → `? \ | = _ + -`。
  **`/` 長撳一彈出嚟就已經停喺 `?`**（`?` 打得多過 `/` 好多），短撳先係 `/`。
- 長撳字母大細階兩樣都揀得到，揀返嚟嗰個帶 `Key.literal`，shift 唔會再覆寫。
- 數字行永遠開住（`Prefs.FORCE_LATIN_NUM_ROW`），設定頁嗰個開關收埋（冇刪 code）。
- `?123` 長撳 = 直接跳純數字頁（中文九宮格嗰粒一樣）。

### 純數字頁

- `中` / `ABC` 搬咗去右上角，`0` `.` `-` 落咗左下角，`⌫` 照舊喺 `⏎` 上面。
- **成頁唔准長撳**（新 `KeyboardBaseView.allowLongPress()` hook）——
  打號碼撳耐咗少少就彈符號 popup 出嚟好煩。

### 中文拉窄 → 側邊欄

- 新 `SidePanelView`：靠左／靠右之下，中文本體窄過螢幕六成
  （`Prefs.SIDE_PANEL_MAX_RATIO`）就收起上面條 bar，功能掣同候選字改為擺喺
  空出嚟嗰邊 —— 上面係功能掣，下面成塊可 scroll 嘅候選字。
- 工具 bar 最左嗰粒掣**左右拖 = 拉闊拉窄**中文本體（新 `Prefs.widthScale`），
  上下拖照舊係拉高拉低。方向跟顯示方式行：拖向留白嗰邊 = 拉闊。
  闊度**唔會**再連累高度（`PadMetrics` 入面 `cellW` / `cellH` 拆開咗計）。

### AI 改寫

- **唔使揀住字都用得**：冇揀就當改寫成個輸入框（出返嚟之前會再全選一次，
  免得等緊嗰陣 user 撳散咗個 selection，變成插字而唔係取代）。
- **未入 API key 就成粒 ✨ 唔見咗**（唔係淨係灰咗）。
- 設定頁個 API key 唔再用輸入框 —— 改成「貼上／刪除／顯示」三粒掣加一行狀態字
  （冇人會逐個字撳 API key）。Prompt 嗰格加咗圓角外框。

### 其他

- 英文／符號頁夾硬開返上面條 bar（唔會改到 `barMode` 本身，返中文照跟返設定）。
- 設定頁「按鍵功能」由「撳一下換一個」改成兩個下拉式選單，亦都唔再重複寫
  「貼上 貼上」（淨係出長名）。
- 收埋咗：「試打」／「預覽」兩段、五條尺寸 slider（按鍵大細／最大闊度／
  最大高度／按鍵高度／鍵盤高度）、「英文鍵盤加一行數字」開關。
  **全部淨係收埋，一行 code 都冇刪**（`SHOW_DEBUG_SECTIONS` /
  `SHOW_HIDDEN_OPTIONS` 改返做 `true` 就出返）。
- app 入面所有畀人睇嘅字由廣東話口語改成正體中文書面語；
  個名一律叫「九万輸入法」／「九万」，「TQ9」淨係留喺 code 度。

## [1.0.6] — 2026-08-23

### 英文滑動

- 單撳唔會再無啦啦變咗短 swipe：要拖夠 `max(鍵闊, 鍵高) × 1.2` 先當滑動
  （`KeyboardBaseView.swipeStartDistPx`，`LatinPadView` override）。拉到隔離格
  咁遠就放手一律當誤觸，照出粒鍵本身 —— qwerty 上面冇兩個字母貼住嘅英文詞。
- 滑動途中喺手指上面浮返「而家掃緊邊粒鍵」出嚟（手指自己遮住咗粒鍵）。

### 長撳彈出嘅變體

- 改用 `PopupWindow`（新 `KeyPopup`），彈得出鍵盤範圍外面：最頂嗰行長撳，
  變體會浮喺手指**上面**（app 嗰邊），唔會再同粒鍵疊埋一舊。
- 變體字大咗 30%，格亦都闊咗少少。

### 鍵盤排位

- 英文：space 同 `.` 中間加咗粒 `?`。
- 符號第一頁：`=\<` 同 `⌫` 搬上倒數第二行嘅最左最右；`/ ? ;` 落咗底行，
  space 右邊而家係 `, . ? ; /` 五粒先至到 `⏎`。
- 符號第二頁：`?123` 同 `⌫` 同樣搬上一行，`numpad` 再升多一行，
  底行剷走 `.` `,`、space 同 `⏎` 拉長（減咗 `∞`）。
- 純數字頁：`⌫` 由右上角搬去 `⏎` 上面，底行左邊兩粒變成 `中` `ABC`。
- 通則：左下兩粒一定係返中文／英文，`⏎` 上面一定係 `⌫`。

---

## [1.0.5] — 2026-08-22

### 改咗

- **中文選字期間，連條線都唔會再畫。** 之前淨係停咗出鍵，條軌跡線仲照畫，
  睇落好似仲滑緊咁。而家 `abortSwipe()` 一次過停晒三樣：唔再派鍵、
  即刻唔畫線、放手嗰下亦都唔會行 `GestureKeyTracker.finish()`
  （嗰下會補返最尾一格，係另一條漏鍵出去嘅路）。

### 加咗

- **選字模式嘅 `0` 向左掃 = 上一頁。** `0` 撳一下係「下頁」，但係一直冇掣返
  上一頁。新加咗一套 **flick** 機制（同 swipe 分開，唔經 `GestureKeyTracker`）：
  `canFlick(key)` 講明邊粒鍵撳住左右掃有意思，`onFlick(key, dx)` 收方向。
  向右冇特別意思，照當撳咗「下頁」。

### 修正

- **開關標點（長撳 `0`）揀住咗字嗰陣，「」而家係包住段字，唔會再蓋咗佢。**
  `commitText` 本身係取代揀咗嗰段，所以要自己 `getSelectedText()` 攞返段字
  接埋落中間：`揀咗嘅字` → `「揀咗嘅字」`。冇揀字嗰陣照舊出一對「」再將
  caret 移返兩個標點中間。

---

## [1.0.4] — 2026-08-22

### 修正

- **慢手由 `7` 一條直線拉去 `9`，唔會再白白多咗個 `8`（`790` 變 `789`）。**
  以前中間格淨係計「喺格入面留咗幾耐」，留夠 `dwellMs` 就俾滿分 —— 即係話
  掃得越慢就越實誤觸，完全反轉。**留得耐 ≠ 撳咗**，慢慢經過一樣會留得耐。
  而家要見到速度真係「跌落去、再彈返上嚟」（V 形）先算數：

  ```
  dipped  = 格入面最慢速度 < 0.35 × 成個 gesture 平均速度
  reaccel = 出格速度 > 最慢速度 × 2
  ```

  即時速度用回望 50ms 嘅**位移**（唔係路程 —— 喺一格度打圈／震手位移細，
  一樣當停低咗）。`GestureKeyTrackerTest` 加咗一對對照 case：同樣慢嘅
  900ms 直線拉唔可以出中間格，同樣慢但真係停一停就要出。

- **滑到中途打夠碼入咗選字模式，跟落嚟嗰啲鍵唔會再攞去揀字／揭頁。**
  滑 `7→9→0`，出到 `79` 已經夠碼出候選，最尾嗰個 `0` 之前走咗去揭第二頁。
  而家 `canSwipe()` 要求 `!engine.selectMode`（未起手就擋），
  `onGestureKey()` 一入選字模式就收手（滑到一半先入就擋）。

---

## [1.0.3] — 2026-08-22

### 修正

- **打網址唔會再喺句號後面硬塞個空格**（`google.com` 唔會再變 `google. com`）。
  句號已經由 auto-space 個表度攞走，淨返 `, ? !`。打嗰一刻根本分唔開
  `google.`（網址）同 `Hello.`（句尾）—— 前面嗰橛一模一樣咁普通，
  試過用 token 內容去估都靠唔住。想斷句就自己撳 ␣。
- URL／email／密碼／`TYPE_TEXT_FLAG_NO_SUGGESTIONS` 嘅欄再加多重保險，
  成個 auto-space 都熄埋（`noAutoSpaceField`）。
- **長撳彈出嘅變體永遠向上彈，唔會再向下。** 英文開咗數字行嗰陣 digits 係
  第 0 行，長撳 `0` 會向下彈到老遠蓋住第二行鍵，撳都撳唔到。
- **長撳之後左右拉，而家真係揀到其他 option。** 之前用「行咗幾多步」嘅相對
  位移嚟揀，但係貼邊嘅鍵（`p`、`0`）成行變體會俾 clamp 迫住向左推，
  睇到嘅高亮同手指位置完全對唔上，變成點拉都揀唔到。改咗做絕對位置
  （手指而家喺邊個格上面就揀邊個）。「長撳完唔郁直接放手 = 打返粒鍵本身」
  照舊 —— 未行夠一個 touch slop 之前一律當第一個。

---

## [1.0.2] — 2026-08-22

### 改咗

- **英文 swipe 認字改用 AOSP 手勢輸入嗰套概念**（`GestureDecoder`）。
  唔再逐格判斷「撳咗邊粒鍵」再砌字母串，而係成條手指軌跡一次過同候選字嘅
  「理想路徑」（逐個字母嘅鍵中心連成線）比對形狀＋位置：兩條軌跡都按弧長
  重新取樣做固定 32 點，逐點計距離，再加首尾點罰分同 `ln(頻率)` 語言模型分。
  距離用鍵闊正規化，唔同螢幕大細都夾到。中文九宮格滑動**冇變**，
  照用返 `GestureKeyTracker` 逐格嗰套。
- `EnDict` 唔再自己做認字，淨返 `fromPrefix` 打字提示同幾個 public accessor。

### 加咗

- **英文選字之後會估下一個字**（N-gram / Trie，AOSP 標準做法）：
  - `EnTrie` — unigram trie，每個節點 bottom-up 快取住自己嗰個 prefix 之下
    常用度最高嘅幾個完整字。
  - `NextWordModel` — bigram（`assets/en_bigram.txt`）做主，冇 context
    或者夾唔到 prefix 就跌落 trie 嘅全域常用字。打緊下一個字嗰陣，
    bigram 夾 prefix 嘅擺前面。
  - `en_bigram.txt` 而家係人手揀嘅常見詞組種子數據，**唔係**真語料統計，
    之後要換成真 corpus 就跟同一個格式重新產生個檔就得。

### 修正

- **打英文（唔係 swipe）唔會再有 underline。** 而家 underline（composing）
  淨係喺啱啱滑完、仲未打過字嗰個短窗口先有；一打字就即刻
  `finishComposingText()` 取消，變返普通已入嘅字。Backspace 同揀候選都
  跟住改返（按字元數刪／取代，唔再當有 composing 區）。

---

## [1.0.1] — 2026-08-21

### 加咗

- email 欄長撳 `@` 可以揀常用信箱域名（`@gmail.com`、`@hotmail.com`），
  第一個照舊係 `@` 本身。

---

## [1.0.0] — 2026-08-21

第一個正式版。九方過期專利 HK1035043 嘅 numpad 中文輸入法，
由 Windows 版（C# WinForms）移植過嚟嘅 Android system keyboard。
